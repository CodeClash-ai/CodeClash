# CoreWar ladder climbed by gpt-5-5. Requires LLAMA_API_KEY in .env.
#   uv run codeclash ladder run configs/ladder/corewar__gpt_5_5.yaml
tournament:
  rounds: 5
ladder_rules: !include ladder/ladder_rules.yaml
game:
  name: CoreWar
  sims_per_round: 2000
player:
  agent: mini
  name: gpt-5-5
  branch_init: human/pspace
  config:
    agent: !include mini/default.yaml
    model: !include mini/models/llama_gpt_5_5.yaml
  push: True
prompts: !include ladder/ladder_prompt.yaml
ladder: !include ladder/rungs/corewar.yaml
